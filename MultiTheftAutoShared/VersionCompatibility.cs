namespace GTANetworkShared
{
    public static class VersionCompatibility
    {
        public static readonly ParseableVersion LastCompatibleClientVersion = new ParseableVersion(0, 0, 0, 0);
        public static readonly ParseableVersion LastCompatibleServerVersion = new ParseableVersion(0, 0, 0, 0);
    }
}