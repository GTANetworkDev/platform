var currentProgressBars = [];

API.onServerEventTrigger.connect(function (eventName, args) {
    if (eventName == "addProgressBar") {
        
    }
});

API.onUpdate.connect(function() {
    for (var i = 0; i < currentProgressBars.length; i++) {
        
    }
});